{
  "private": true,
  "name": "kenta",
  "version": "1.0.0",
  "description": "root project for monorepo management",
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "backoffice",
    "packages/*"
  ],
  "scripts": {    
    "build-test": "yarn workspaces backoffice run build",
    "build": "yarn workspaces run clean && yarn workspaces run build",
    "clean": "rm -rf ./dist && rm -rf tsconfig.build.tsbuildinfo",
    "compile": "tsc -b tsconfig.build.json"
  }
}
